// Write code under this line
const calculateTotalBalance = (array) => {
  return array.reduce((acc, array) => acc + array.balance, 0);
};

function array({ balance }) {
  return balance;
}

console.log(calculateTotalBalance(users));
// 20916
